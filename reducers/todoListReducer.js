// import action variables

const intialState = {
    todos: [],
    error: '',
    pending: false
}

export const todoList = (state = intialState, action) => {

    switch(action.type) {

        default:
            return state;
    }
}